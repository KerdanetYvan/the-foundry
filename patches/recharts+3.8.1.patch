diff --git a/node_modules/recharts/lib/state/legendSlice.js b/node_modules/recharts/lib/state/legendSlice.js
index 4ca008e..79b9e83 100644
--- a/node_modules/recharts/lib/state/legendSlice.js
+++ b/node_modules/recharts/lib/state/legendSlice.js
@@ -43,7 +43,13 @@ var legendSlice = (0, _toolkit.createSlice)({
     },
     addLegendPayload: {
       reducer(state, action) {
-        state.payload.push((0, _immer.castDraft)(action.payload));
+        var dataKey = action.payload[0] && action.payload[0].dataKey;
+        var existing = state.payload.findIndex(function(arr) { return arr[0] && arr[0].dataKey === dataKey; });
+        if (existing > -1) {
+          state.payload[existing] = (0, _immer.castDraft)(action.payload);
+        } else {
+          state.payload.push((0, _immer.castDraft)(action.payload));
+        }
       },
       prepare: (0, _toolkit.prepareAutoBatched)()
     },
@@ -53,7 +59,8 @@ var legendSlice = (0, _toolkit.createSlice)({
           prev,
           next
         } = action.payload;
-        var index = (0, _toolkit.current)(state).payload.indexOf((0, _immer.castDraft)(prev));
+        var dataKey = prev[0] && prev[0].dataKey;
+        var index = state.payload.findIndex(function(arr) { return arr[0] && arr[0].dataKey === dataKey; });
         if (index > -1) {
           state.payload[index] = (0, _immer.castDraft)(next);
         }
@@ -62,7 +69,8 @@ var legendSlice = (0, _toolkit.createSlice)({
     },
     removeLegendPayload: {
       reducer(state, action) {
-        var index = (0, _toolkit.current)(state).payload.indexOf((0, _immer.castDraft)(action.payload));
+        var dataKey = action.payload[0] && action.payload[0].dataKey;
+        var index = state.payload.findIndex(function(arr) { return arr[0] && arr[0].dataKey === dataKey; });
         if (index > -1) {
           state.payload.splice(index, 1);
         }
diff --git a/node_modules/recharts/lib/state/tooltipSlice.js b/node_modules/recharts/lib/state/tooltipSlice.js
index d2e59b4..4384943 100644
--- a/node_modules/recharts/lib/state/tooltipSlice.js
+++ b/node_modules/recharts/lib/state/tooltipSlice.js
@@ -120,7 +120,13 @@ var tooltipSlice = (0, _toolkit.createSlice)({
   reducers: {
     addTooltipEntrySettings: {
       reducer(state, action) {
-        state.tooltipItemPayloads.push((0, _immer.castDraft)(action.payload));
+        var dataKey = action.payload.settings.dataKey;
+        var existing = state.tooltipItemPayloads.findIndex(function(item) { return item.settings.dataKey === dataKey; });
+        if (existing > -1) {
+          state.tooltipItemPayloads[existing] = (0, _immer.castDraft)(action.payload);
+        } else {
+          state.tooltipItemPayloads.push((0, _immer.castDraft)(action.payload));
+        }
       },
       prepare: (0, _toolkit.prepareAutoBatched)()
     },
@@ -130,7 +136,8 @@ var tooltipSlice = (0, _toolkit.createSlice)({
           prev,
           next
         } = action.payload;
-        var index = (0, _toolkit.current)(state).tooltipItemPayloads.indexOf((0, _immer.castDraft)(prev));
+        var dataKey = prev.settings.dataKey;
+        var index = state.tooltipItemPayloads.findIndex(function(item) { return item.settings.dataKey === dataKey; });
         if (index > -1) {
           state.tooltipItemPayloads[index] = (0, _immer.castDraft)(next);
         }
@@ -139,7 +146,8 @@ var tooltipSlice = (0, _toolkit.createSlice)({
     },
     removeTooltipEntrySettings: {
       reducer(state, action) {
-        var index = (0, _toolkit.current)(state).tooltipItemPayloads.indexOf((0, _immer.castDraft)(action.payload));
+        var dataKey = action.payload.settings.dataKey;
+        var index = state.tooltipItemPayloads.findIndex(function(item) { return item.settings.dataKey === dataKey; });
         if (index > -1) {
           state.tooltipItemPayloads.splice(index, 1);
         }
